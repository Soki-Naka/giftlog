<% provide(:title, @title) %>
  <div>
    <h1 class="text-center"><%= @user.name %></h1>
    <div class="text-center mb-5">
      <%= "#{@user.following.count}フォロー" %> | <%= link_to "#{@user.followers.count}フォロワー", followers_user_path(@user) %>
    </div>
    <h1 class="text-center"><%= @title %></h3>
    <% if @users.any? %>
      <%= render @users %>
      <%= paginate @users, theme: 'twitter-bootstrap-4' , pagination_class: "justify-content-center" %>
    <% else %>
      <p class="text-center">フォローしたユーザーはいません。<br>ユーザーリストからフォローしてみよう！</p>
    <% end %>
  </div>